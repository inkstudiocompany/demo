(function ($) {
    $.fn.extend({
        endpoints: {
            widgets         :    'ajax/widgets',
            widgets_form    :    'ajax/widgets/form',
            blocks          :    'ajax/blocks',
        },
    });
})(jQuery);